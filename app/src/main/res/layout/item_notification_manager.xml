<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools">

    <ImageView
        android:src="@drawable/beach"
        android:id="@+id/item_notification_manager_imageview"
        android:layout_marginVertical="22dp"
        android:layout_marginStart="26dp"
        android:layout_width="44dp"
        android:layout_height="44dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/item_notification_manager_user"
        android:gravity="center"
        android:textColor="@color/colorText"
        tools:text="Ligue 1"
        android:textSize="17sp"
        android:layout_marginStart="15dp"
        app:layout_constraintStart_toEndOf="@id/item_notification_manager_imageview"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Switch
        android:paddingVertical="6dp"
        android:paddingHorizontal="24dp"
        android:gravity="center"
        android:layout_marginEnd="15dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:id="@+id/item_notification_manager_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>


</androidx.constraintlayout.widget.ConstraintLayout>