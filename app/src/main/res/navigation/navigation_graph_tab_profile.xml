<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation_graph_tab_4"
    app:startDestination="@id/profile">
    <fragment
        android:id="@+id/profile"
        android:name="com.timenoteco.timenote.view.Profile"
        android:label="fragment_profile"
        tools:layout="@layout/fragment_profile" >
        <action
            android:id="@+id/action_profile_to_profilModify"
            app:destination="@id/profilModify" />
    </fragment>
    <fragment
        android:id="@+id/profilModify"
        android:name="com.timenoteco.timenote.view.ProfilModify"
        android:label="ProfilModify" >
        <action
            android:id="@+id/action_profilModify_to_profile2"
            app:popUpTo="@id/profile" />
    </fragment>
</navigation>